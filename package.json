{
  "name": "server",
  "version": "0.1.5",
  "private": true,
  "scripts": {
    "start": "node ./back-end/bin/www",
    "heroku-postbuild": "cd back-end && npm install && cd ../front-end && npm install && npm run build"
  },
  "engines": {
    "node": "12.14.1"
  }
}
